# Production override: use .env.prod for the web service.
# Run: docker compose --env-file .env.prod -f docker-compose.yml -f docker-compose.prod.yml up -d
# Ensure .env.prod has real SECRET_KEY, DEBUG=false, ALLOWED_HOSTS, and DB_PASSWORD.

services:
  web:
    env_file: .env.prod
